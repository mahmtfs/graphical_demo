/*#pragma once
#include "../../includes/config/fmconfig/fmconfig.h"

class FileManager {
	static FileManager* instance;
	FileManager();
public:
	static FileManager* getInstance(FMConfig config);
};
*/