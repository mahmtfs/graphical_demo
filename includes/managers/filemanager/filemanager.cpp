/*#include "filemanager.h"

FileManager::FileManager() {
	getInstance();
}

FileManager* FileManager::getInstance(FMConfig config) {
	if (!instance) {
		instance = new FileManager();
	}
	return instance;
}
*/